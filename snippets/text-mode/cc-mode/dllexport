# -*- mode: snippet -*-
# name: dllexport
# key: dllexport
# --
#ifdef _WIN32
# include <windows.h>
# ifdef ${1:DLL_EXPORT}
#  ifndef ${2:LIB_API}
#   define $2  __declspec(dllexport)
#  endif
# else
#  ifndef $2
#   define $2  __declspec(dllimport)
#  endif
# endif
#else   /* non-windows */
# define $2
#endif
$0